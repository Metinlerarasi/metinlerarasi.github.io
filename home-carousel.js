(()=>{const stage=document.querySelector('.chosen-book'),slides=[...document.querySelectorAll('.spotlight-slide')];if(slides.length<2)return;const feature=stage.closest('.feature-stage');const progress=document.createElement('div');progress.className='spotlight-progress';progress.innerHTML='<span class="progress-label">KİTAP</span><i><b></b></i><span class="progress-next">FİLM</span>';feature?.appendChild(progress);let active=0,timer;const restartProgress=()=>{const bar=progress.querySelector('b');bar.style.animation='none';void bar.offsetWidth;bar.style.animation='spotlightFill 5s linear forwards'};const show=index=>{active=index;slides.forEach((slide,i)=>{slide.classList.toggle('active',i===active);slide.setAttribute('aria-hidden',String(i!==active))});stage.dataset.spotlight=active?'film':'book';progress.querySelector('.progress-label').textContent=active?'FİLM':'KİTAP';progress.querySelector('.progress-next').textContent=active?'KİTAP':'FİLM';restartProgress()};const pause=()=>{clearInterval(timer);progress.classList.add('paused')};const start=()=>{clearInterval(timer);progress.classList.remove('paused');restartProgress();timer=setInterval(()=>show((active+1)%slides.length),5000)};stage.addEventListener('mouseenter',pause);stage.addEventListener('mouseleave',start);stage.addEventListener('focusin',pause);stage.addEventListener('focusout',start);show(0);start()})();
