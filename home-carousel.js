(()=>{const stage=document.querySelector('.chosen-book'),slides=[...document.querySelectorAll('.spotlight-slide')];if(slides.length<2)return;let active=0,timer;const show=index=>{active=index;slides.forEach((slide,i)=>{slide.classList.toggle('active',i===active);slide.setAttribute('aria-hidden',String(i!==active))});stage.dataset.spotlight=active?'film':'book'};const start=()=>{clearInterval(timer);timer=setInterval(()=>show((active+1)%slides.length),5000)};stage.addEventListener('mouseenter',()=>clearInterval(timer));stage.addEventListener('mouseleave',start);stage.addEventListener('focusin',()=>clearInterval(timer));stage.addEventListener('focusout',start);show(0);start()})();
